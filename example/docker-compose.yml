version: "2"

services:
  android-sdk:
    image: runmymind/docker-android-sdk:feature_example-and-locking-alpine-lazydl
    volumes:
        - android-sdk:/opt/android-sdk-linux
    restart: always

  java:
    image: runmymind/docker-android-sdk:feature_example-and-locking-example
    volumes:
        - android-sdk:/opt/android-sdk-linux
    restart: always

volumes:
    android-sdk: